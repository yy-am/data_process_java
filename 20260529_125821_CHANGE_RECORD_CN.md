# 2026-05-29 12:58:21 变更记录

## 变更范围

本次变更聚焦数据加工 ReAct Agent 的提示词、工具描述和 skill 执行说明，目标是让 Agent 在中文语境下更稳定地执行“解析 Excel 后推进到用户确认阶段”的流程。

## 主要改动

1. Agent 基础提示词中文化

- 将 `DataProcessingReactAgentConfig` 中的 Agent `description` 和 `instruction` 从英文改为中文。
- 明确要求 Agent 首先调用 `read_skill` 读取 `data-processing-agent-skill`。
- 明确当前测试范围只推进到 `USER_CONFIRMATION_REQUIRED` 或 `USER_CONFIRMED`。
- 明确当前阶段不得进入临时表落库、SQL 片段生成、SQL 拼接或结果表写入。

2. 工具描述中文化

- 将 `DataProcessingAgentToolMethods` 中所有 `@Tool` 的 `description` 改为中文。
- 将工具入参 `@ToolParam` 的说明改为中文。
- 保留工具名称不变，继续使用已有英文 tool name，避免影响 Agent 工具调用协议和既有代码引用。

3. Skill 描述中文化

- 将 `data-processing-agent-skill` 的 frontmatter `description` 改为中文。

4. 修正第 4 步字段绑定计划的规则边界

- 明确第 4 步只为需要 Excel 原始列的规则生成字段绑定计划。
- 需要字段绑定的规则类型限定为：
  - `DIRECT_MAPPING`
  - `EXPR`
- 明确 `USER_CONFIRM_OPTION` 和 `USER_CONFIRM_INPUT` 不参与字段绑定。
- 明确字段绑定计划只描述“规则源字段到 Excel 原始列”的映射关系。
- 明确字段绑定状态只允许：
  - `CONFIRMED`
  - `NEEDS_CONFIRMATION`
  - `MISSING`
- 明确 `USER_CONFIRM_OPTION` 和 `USER_CONFIRM_INPUT` 不是字段绑定状态，也不是字段绑定计划中的规则源字段状态。

5. 修正第 5 步用户确认项生成输入

- 明确第 5 步必须同时消费：
  - 第 4 步保存的字段绑定计划。
  - 第 4 步分流出的 `USER_CONFIRM_OPTION` 规则。
  - 第 4 步分流出的 `USER_CONFIRM_INPUT` 规则。
  - 标准模板必填字段定义。
- 明确不得因为 `USER_CONFIRM_OPTION` 或 `USER_CONFIRM_INPUT` 没有出现在字段绑定计划中，就遗漏对应确认项。
- 明确 `USER_CONFIRM_OPTION` 生成值集选择确认项，且该确认项来自目标列取值来源，不来自字段绑定计划。
- 明确 `USER_CONFIRM_INPUT` 天然生成手工输入确认项，不需要 Excel 字段绑定。

## 影响说明

- 本次没有改变工具方法名、Agent Bean 名称或对外服务入口。
- 本次没有新增业务阶段，也没有进入临时表和 SQL 执行相关实现。
- 本次主要降低 Agent 对字段绑定计划的误解风险，避免把 `USER_CONFIRM_OPTION` / `USER_CONFIRM_INPUT` 错误地当成字段绑定状态或字段绑定项处理。

## 验证说明

- 本次变更包含文档和提示词调整，未执行完整 Maven 编译。
- 当前项目仍存在与本次无关的工作树改动；提交时仅纳入本次 agent/skill 相关文件和本变更记录。
