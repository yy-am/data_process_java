# 2026-05-30 16:06:17 确认后上下文准备与 SQL 渲染状态修正变更记录

## 变更背景

确认完成后的 `USER_CONFIRMED` 只代表用户确认结果已经保存，并不代表可以直接进入 SQL 渲染完成状态。进入 `PROCESSING_SQL_RENDERED` 的前提必须是 `execute_processing_plan` 已成功执行，并且已经具备 `sqlGenerationContext` 和 `processingPlanDsl` 两类输入。

原 skill 中第 4 步和第 5 步错误包含 `PROCESSING_SQL_RENDERED` 返回分支，容易误导 Agent 认为字段绑定或用户确认提交后可以直接进入 SQL 渲染完成态。同时，确认后直接调用 `prepare_sql_generation_context` 前缺少一个确定性前置步骤，用于校验确认后的加工上下文是否已经完整可用。

## 新增

### `src/main/java/com/example/dataprocess/agent/model/AgentWorkflowStage.java`

- 在枚举 `AgentWorkflowStage` 中新增阶段：
  - `POST_CONFIRMATION_CONTEXT_READY`
- 阶段含义：
  - 用户确认完成后，已通过确定性工具校验模板、规则、字段绑定计划和用户确认结果均已齐备。
  - 该阶段是调用 `prepare_sql_generation_context(taskId)` 的前置条件。

### `src/main/java/com/example/dataprocess/agent/model/DataProcessingAgentState.java`

- 在记录类 `DataProcessingAgentState` 中新增字段：
  - `RenderedProcessingSql renderedProcessingSql`
- 新增方法：
  - `withRenderedProcessingSql(RenderedProcessingSql value)`
- 用途：
  - 持久化 `execute_processing_plan` 成功后生成的完整 SQL 渲染结果。
  - 保证恢复到 `PROCESSING_SQL_RENDERED` 阶段时，`summary` 中可以返回 `resultTable`、`stagingTable`、`insertSelectSql`、`loadedRows` 等信息。

### `src/main/java/com/example/dataprocess/agent/tool/DataProcessingAgentToolMethods.java`

- 新增暴露给 Agent 的工具方法：
  - `preparePostConfirmationContext(String taskId)`
- 对应工具名：
  - `prepare_post_confirmation_context`
- 工具职责：
  - 校验当前阶段只能是 `USER_CONFIRMED` 或 `POST_CONFIRMATION_CONTEXT_READY`。
  - 校验 `parsedExcelSummary` 已存在。
  - 校验 `templateRecognitionResult` 已存在。
  - 校验 `templateBundle.processingRule` 已存在。
  - 校验 `fieldBindingPlan.items` 已存在且非空。
  - 如果存在 `confirmationItems`，校验 `userConfirmationResult` 完整覆盖全部确认项。
  - 校验通过后将阶段推进到 `POST_CONFIRMATION_CONTEXT_READY`。
  - 返回 `readyForSqlGeneration=true`、`nextAction` 和标准 `agentResponse`。

- 新增私有校验方法：
  - `validatePostConfirmationContext(DataProcessingAgentState state)`
- 方法职责：
  - 为 `preparePostConfirmationContext(...)` 提供确定性上下文完整性校验。

## 修改

### `src/main/java/com/example/dataprocess/agent/model/DataProcessingAgentState.java`

- 修改类注释：
  - 从“只覆盖到用户确认”调整为“可恢复的数据加工执行状态”。
- 修改 `initial(String taskId, String parsedFileRef)`：
  - 初始化时新增 `renderedProcessingSql=null`。
- 修改状态复制链路：
  - `withStage(...)`
  - `withParsedExcelSummary(...)`
  - `withTemplateRecognitionResult(...)`
  - `withTemplateContext(...)`
  - `withFieldBindingPlan(...)`
  - `withConfirmationItems(...)`
  - `withUserConfirmationResult(...)`
  - `addTrace(...)`
  - `addError(...)`
  - `copy(...)`
- 修改目的：
  - 保证新增的 `renderedProcessingSql` 字段在状态复制和阶段推进过程中不会丢失。
- 修改 `summary()`：
  - 原先只返回 `traceLogs` 和 `errorMessages`。
  - 现在当 `renderedProcessingSql` 存在时，额外返回：
    - `resultTable`
    - `stagingTable`
    - `targetColumns`
    - `insertSelectSql`
    - `loadedRows`

### `src/main/java/com/example/dataprocess/agent/tool/DataProcessingAgentToolMethods.java`

- 修改方法 `executeProcessingPlan(String taskId, AgentSqlGenerationContext sqlGenerationContext, ProcessingPlanDsl processingPlanDsl)`：
  - 在 `ProcessingPlanSqlTool.renderInsertSelectSql(...)` 成功后，将返回的 `RenderedProcessingSql` 写入 `DataProcessingAgentState.renderedProcessingSql`。
  - 然后将任务阶段推进到 `PROCESSING_SQL_RENDERED`。
  - 保证 `PROCESSING_SQL_RENDERED` 阶段具备可恢复的 SQL 渲染结果。

- 修改方法 `nextAction(DataProcessingAgentState state)`：
  - `USER_CONFIRMED` 的下一步从直接调用 `prepare_sql_generation_context` 改为调用 `prepare_post_confirmation_context`。
  - 新增 `POST_CONFIRMATION_CONTEXT_READY` 分支，提示调用 `prepare_sql_generation_context`。

- 修改方法 `responseMessage(DataProcessingAgentState state)`：
  - 新增 `POST_CONFIRMATION_CONTEXT_READY` 的响应文案：
    - `确认后的加工上下文已准备完成。`

### `src/main/resources/agent/skills/data-processing-agent-skill/SKILL.md`

- 修改“可调用工具”章节：
  - 新增确认后工具 `prepare_post_confirmation_context(taskId)`。
  - 将 `prepare_sql_generation_context(taskId)` 调整为只能在 `POST_CONFIRMATION_CONTEXT_READY` 阶段调用。
  - 将 `execute_processing_plan(...)` 的描述改为接收 `prepare_sql_generation_context` 返回的 SQL 生成上下文。

- 修改“阶段含义”：
  - 新增 `POST_CONFIRMATION_CONTEXT_READY` 阶段说明。

- 修改“第 1 步：准备任务上下文”的阶段分支：
  - `USER_CONFIRMED` 进入第 6 步。
  - `POST_CONFIRMATION_CONTEXT_READY` 进入第 7 步。
  - `SQL_GENERATION_CONTEXT_READY` 进入第 8 步。

- 修改“第 4 步：提交字段绑定计划”的返回分支：
  - 删除不合理的 `PROCESSING_SQL_RENDERED` 分支。
  - 第 4 步只允许处理 `USER_CONFIRMATION_REQUIRED`、`USER_CONFIRMED`、`FAILED`。

- 修改“第 5 步：处理用户确认提交”的返回分支：
  - 删除不合理的 `PROCESSING_SQL_RENDERED` 分支。
  - 第 5 步只允许处理 `USER_CONFIRMED`、`USER_CONFIRMATION_REQUIRED`、`FAILED`。

- 新增“第 6 步：准备确认后的加工上下文”：
  - 明确必须调用 `prepare_post_confirmation_context(taskId)`。
  - 明确该步骤成功后才进入 `POST_CONFIRMATION_CONTEXT_READY`。
  - 明确该步骤不写临时表、不生成 SQL，只校验确认后的加工上下文。

- 原“第 6 步：落临时表并准备 SQL 生成上下文”调整为第 7 步：
  - 明确仅当阶段为 `POST_CONFIRMATION_CONTEXT_READY` 时执行。

- 原“第 7 步：生成目标列 SQL 表达式片段计划”调整为第 8 步。
- 原“第 8 步：提交加工计划并写入结果表”调整为第 9 步。

## 删除

- 删除 skill 第 4 步中关于 `PROCESSING_SQL_RENDERED` 的错误返回分支。
- 删除 skill 第 5 步中关于 `PROCESSING_SQL_RENDERED` 的错误返回分支。
- 本次未删除任何 Java 类或文件。

## 未包含内容

- 本次提交不包含流式响应相关改动。
- 本次提交不包含其他非 Agent 确认后上下文流程相关的工作区变更。

## 验证情况

- 已对以下 Agent 相关 Java 文件执行窄编译验证：
  - `AgentWorkflowStage.java`
  - `DataProcessingAgentState.java`
  - `DataProcessingReactAgentService.java`
  - `DataProcessingAgentToolMethods.java`
- 窄编译通过。
- 全量 Maven 编译仍受既有 `DataProcessingStateGraphDefinition.java` 语法错误影响，未作为本次变更处理范围。
