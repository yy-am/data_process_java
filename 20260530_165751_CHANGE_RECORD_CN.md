# 2026-05-30 16:57:51 变动说明

## 背景

本次变更围绕 React Agent 数据加工流程中“用户确认后到 SQL 生成”的上下文契约进行收敛：

- `prepare_sql_generation_context` 只返回确定性的 SQL 表上下文。
- 业务加工上下文由 `prepare_post_confirmation_context` 和任务状态提供。
- 删除 agent 新流程中无实际停留价值的 `TEMPLATE_RECOGNIZED` 阶段。
- 修正 skill 中关于 `USER_CONFIRMED`、SQL 生成上下文和执行步骤的描述。

## 新增内容

### `DataProcessingAgentToolMethods`

新增私有辅助方法，用于在工具内部从任务状态构造 DSL 校验与生成所需上下文：

- `buildDslBusinessContext(DataProcessingAgentState state)`
  - 将目标列、规则类型、已确认映射、规则说明、示例、用户确认固定值整理为 Agent 生成 `processingPlanDsl` 可使用的业务上下文。
- `buildDslGenerationContext(DataProcessingAgentState state, AgentSqlGenerationContext sqlGenerationContext)`
  - 基于任务状态和 SQL 表上下文构造确定性 `DslGenerationContext`，供工具内部校验 `processingPlanDsl` 使用。
- `buildTargetColumnGenerationContexts(DataProcessingAgentState state, List<ActualColumnMapping> columnMappings)`
  - 将加工规则、字段绑定计划、用户确认结果和临时表字段映射合并成目标列生成上下文。
- `resolveActualColumnMappings(...)`
  - 根据字段绑定项和用户确认结果解析目标列对应的原始 Excel 列映射。
- `resolveActualColumnMapping(...)`
  - 将 Excel 原始列转换为临时表弹性字段映射；在 SQL 映射已存在但缺失目标列时抛出错误。
- `selectedHeader(...)`
  - 从已确认绑定或用户映射确认结果中提取最终 Excel 列名。
- `confirmedValue(...)`
  - 从值集选择或手工输入确认结果中提取目标列固定值。
- `sourceColumnFromKey(...)`
  - 从确认项 key 中解析规则源字段。
- `safeFieldBindings(...)`、`safeDecisions(...)`、`safeColumnMappings(...)`
  - 对可空集合做空安全处理。

## 修改内容

### `src/main/java/com/example/dataprocess/agent/model/AgentSqlGenerationContext.java`

- 修改 `AgentSqlGenerationContext` 的职责说明：
  - 从“SQL 生成上下文”收敛为“SQL 表上下文”。
- 保留字段：
  - `taskId`
  - `stagingTable`
  - `resultTable`
  - `loadedRows`
  - `columnMappings`
- 删除字段：
  - `DslGenerationContext dslGenerationContext`
- 删除对应 import：
  - `com.example.dataprocess.domain.model.DslGenerationContext`

### `src/main/java/com/example/dataprocess/agent/model/AgentWorkflowStage.java`

- 删除枚举值：
  - `TEMPLATE_RECOGNIZED`
- 调整 `CONFIRMATION_ANALYZED` 注释，去掉“保留阶段”的表述，使状态定义更贴近当前 agent 流程。

### `src/main/java/com/example/dataprocess/agent/tool/DataProcessingAgentToolMethods.java`

#### `preparePostConfirmationContext(String taskId)`

- 扩展返回内容，新增：
  - `templateBundle`
  - `requiredFields`
  - `valueSetMetadata`
  - `fieldBindingPlan`
  - `dslBusinessContext`
- 目的：
  - 让 Agent 在第 6 步之后具备生成 `processingPlanDsl` 的业务上下文。
  - 避免第 7 步 `prepare_sql_generation_context` 重复返回模板、规则、字段绑定和用户确认结果。

#### `executeProcessingPlan(String taskId, AgentSqlGenerationContext sqlGenerationContext, ProcessingPlanDsl processingPlanDsl)`

- 调整 `sqlGenerationContext` 参数说明：
  - 从“SQL 生成上下文”改为“SQL 表上下文”。
- 调用 `ProcessingPlanSqlTool.renderInsertSelectSql(...)` 时新增内部构造的 `DslGenerationContext`：
  - `buildDslGenerationContext(requiredState(taskId), sqlGenerationContext)`
- 目的：
  - `processingPlanDsl` 的校验上下文由工具基于任务状态确定性构造。
  - 不再要求 Agent 把模板、规则、字段绑定计划或用户确认结果塞进 `AgentSqlGenerationContext`。

#### `saveTemplateRecognition(String taskId, TemplateRecognitionResult result)`

- 删除保存模板识别结果后设置 `TEMPLATE_RECOGNIZED` 阶段的逻辑。
- 模板识别结果保存后仍保留 trace，后续由模板上下文加载工具进入 `TEMPLATE_CONTEXT_READY`。

#### `nextAction(DataProcessingAgentState state)`

- 删除 `TEMPLATE_RECOGNIZED` 分支。
- `TEMPLATE_CONTEXT_READY` 继续指向字段绑定计划生成和校验流程。

#### `responseMessage(DataProcessingAgentState state)`

- 删除 `TEMPLATE_RECOGNIZED` 的响应文案。

### `src/main/java/com/example/dataprocess/agent/tool/ProcessingPlanSqlTool.java`

#### `renderInsertSelectSql(...)`

- 方法签名调整为：
  - `renderInsertSelectSql(String taskId, AgentSqlGenerationContext context, DslGenerationContext dslGenerationContext, ProcessingPlanDsl processingPlanDsl)`
- `ProcessingPlanDslValidator.validate(...)` 改为使用独立传入的 `DslGenerationContext`。
- 目的：
  - SQL 表上下文与 DSL 业务校验上下文解耦。

#### `validateContext(...)`

- 方法签名调整为：
  - `validateContext(String taskId, AgentSqlGenerationContext context, DslGenerationContext dslGenerationContext)`
- 校验内容调整：
  - 校验 `AgentSqlGenerationContext` 不为空。
  - 校验 `context.taskId()` 与工具入参一致。
  - 校验 `DslGenerationContext` 不为空。
  - 校验 `dslGenerationContext.taskId()` 与工具入参一致。
  - 保留表名和字段映射校验。

### `src/main/resources/agent/skills/data-processing-agent-skill/SKILL.md`

#### 工具契约调整

- `prepare_post_confirmation_context`
  - 明确成功后返回 `POST_CONFIRMATION_CONTEXT_READY`。
  - 明确返回生成 `processingPlanDsl` 所需业务上下文。
- `prepare_sql_generation_context`
  - 明确只返回 SQL 表上下文。
  - 明确返回结构：
    - `taskId`
    - `stagingTable`
    - `resultTable`
    - `loadedRows`
    - `columnMappings`
  - 明确不得重复返回模板、规则、字段绑定计划或用户确认结果。
- `execute_processing_plan`
  - 明确工具内部基于 `taskId` 从任务状态读取业务加工上下文。
  - 明确工具内部负责 DSL 校验、SQL 片段安全校验和完整 SQL 拼接。

#### 阶段说明调整

- 修正 `USER_CONFIRMED` 定义：
  - 该阶段表示确认条件已经满足。
  - 可能是前端用户已提交确认结果。
  - 也可能是工具判断本任务无需用户确认。
  - 它不是最终完成阶段，而是第 6 步之后流程的起点。
- 删除 `TEMPLATE_RECOGNIZED` 阶段说明。

#### 执行步骤调整

- 第 1 步阶段分支中删除 `TEMPLATE_RECOGNIZED`。
- 第 6 步补充：
  - 必须确认工具返回了生成 `processingPlanDsl` 所需的业务加工上下文。
- 第 7 步补充：
  - 必须返回 `resultTable`。
  - 不得返回模板、规则、字段绑定计划或用户确认结果。
- 第 8 步补充：
  - Agent 生成 `processingPlanDsl` 时必须同时使用：
    - 第 6 步返回的业务加工上下文。
    - 第 7 步返回的 SQL 表上下文。
- 第 9 步补充：
  - `execute_processing_plan` 基于 `taskId` 从任务状态重建 DSL 校验上下文。
  - 不接受 Agent 传入模板、规则、字段绑定计划或用户确认结果作为校验依据。

## 删除内容

- 删除 agent 新流程中的 `AgentWorkflowStage.TEMPLATE_RECOGNIZED`。
- 删除 `AgentSqlGenerationContext.dslGenerationContext` 字段。
- 删除 skill 中对 `TEMPLATE_RECOGNIZED` 的阶段描述和跳转说明。
- 删除工具响应文案和下一步动作中的 `TEMPLATE_RECOGNIZED` 分支。

## 验证情况

- 已检查 agent 目录和 skill 文档中不存在残留引用：
  - `TEMPLATE_RECOGNIZED`
  - `dslGenerationContext()`
- 已使用窄范围 `javac` 对本次涉及的 agent 类完成编译检查。
- 未执行完整 Maven 编译，因为当前项目中 `DataProcessingStateGraphDefinition.java` 存在既有语法错误，会阻断全量编译。

## 注意事项

- 本次提交不包含之前实验性的流式响应相关代码。
- `prepare_sql_generation_context` 的实现方需要按本文档中 `AgentSqlGenerationContext` 的新结构返回 SQL 表上下文。
