# 2026-05-29 14:37:56 变更记录

## 变更背景

调试数据加工 ReAct Agent 时发现，Agent 已经选择调用 `load_template_catalog` 工具，但没有进入 `DataProcessingAgentToolMethods.loadTemplateCatalog()` 方法内部断点。

随后 Agent 观察到工具调用失败，错误信息为 `toolInput cannot be null or empty`。

## 问题定位

`load_template_catalog` 对应的 Java 方法是无参方法：

```java
public String loadTemplateCatalog()
```

但在 ReAct Agent 工具执行链中，模型生成的 tool call arguments 可能是 `null` 或空字符串。

Spring AI `MethodToolCallback.call(String toolInput)` 在真正反射调用工具方法前，会先执行非空校验：

```java
Assert.hasText(toolInput, "toolInput cannot be null or empty");
```

因此当 arguments 为空时，执行会在 Spring AI 工具回调层提前失败，业务工具方法不会被调用，断点也不会命中。

对比 Spring AI 默认 `DefaultToolCallingManager` 的执行逻辑，其会在工具 arguments 为空时兜底为 `{}`。当前 Spring AI Alibaba ReAct Agent 的 `AgentToolNode` 直接透传 `ToolCallRequest.arguments`，没有做这个兼容处理。

## 修复方案

新增 `BlankToolInputNormalizingInterceptor`，在 Agent 工具执行进入具体 `ToolCallback` 前统一规范化工具入参：

- 如果 `ToolCallRequest.arguments` 为 `null` 或空白字符串，则替换为 `{}`。
- 如果 arguments 非空，则保持原值不变。

该修复放在 ToolInterceptor 层，而不是包装单个业务工具或修改无参工具签名，原因是：

- 这是 ReAct Agent 工具执行链和 Spring AI 默认工具执行链之间的通用兼容问题。
- 未来新增其它无参工具时也能复用该行为。
- 不改变原始 `ToolCallback` 类型，不影响框架对 `MethodToolCallback`、`FunctionToolCallback`、`StateAwareToolCallback` 等类型的判断。

## 代码改动

1. 新增 `BlankToolInputNormalizingInterceptor`

- 文件：`src/main/java/com/example/dataprocess/agent/config/BlankToolInputNormalizingInterceptor.java`
- 职责：在工具调用进入下一个 handler 前，将空白工具入参统一替换为 `{}`。

2. 调整 `DataProcessingReactAgentConfig`

- 在 `.interceptors(...)` 中加入 `BlankToolInputNormalizingInterceptor`。
- 将其放在 `ToolErrorInterceptor` 前面，使空入参先被规范化，再进入错误兜底拦截器和真实工具执行。

## 影响范围

- 修复 `load_template_catalog` 等无参工具在 ReAct Agent 中因空 arguments 无法执行的问题。
- 不改变已有工具名称、工具方法签名、skill 内容或 Agent 服务入口。
- 对有参工具无影响；有参工具 arguments 非空时会原样透传。

## 验证说明

已使用当前 agent 相关 classpath 对以下文件执行 `javac` 编译检查并通过：

- `BlankToolInputNormalizingInterceptor.java`
- `DataProcessingReactAgentConfig.java`

完整 Maven 编译未执行；当前工作树仍存在与本次修复无关的其它未提交改动。
