# 2026-05-29 17:06:15 变更记录

## 变更背景

原先 `DataProcessingReactAgentConfig` 使用 `FileSystemSkillRegistry` 从源码目录读取 skill：

```java
src/main/resources/agent/skills
```

该路径适合本地源码运行，但 Docker 或 jar 发包后运行目录中通常不存在 `src/main/resources`，导致服务启动时找不到 skill 目录。

## 本次调整

将数据加工 Agent 的 skill 加载方式从文件系统源码路径切换为 classpath 加载。

核心配置改为：

```java
ClasspathSkillRegistry.builder()
        .classpathPath("agent/skills")
        .basePath(SKILL_CACHE_PATH.toString())
        .build();
```

其中：

- `classpathPath` 固定为 `agent/skills`，对应 `src/main/resources/agent/skills` 打包后的 classpath 位置。
- `basePath` 使用系统临时目录下的 `data-processing-agent-skill-cache`，用于 `ClasspathSkillRegistry` 在 jar 环境中拷贝和缓存 skill 资源。
- 不再读取 `data-processing.agent.skills-path` 或其它 yaml 配置。

## 影响范围

- Docker/jar 运行时不再依赖源码目录 `src/main/resources/agent/skills`。
- skill 会随应用一起打包发布。
- 修改 skill 内容后需要重新打包发布，不再支持仅替换外部文件系统目录来热更新 skill。
- 保留原有 skill 名称 `data-processing-agent-skill` 和工具绑定方式，不影响 Agent 调用协议。

## 验证说明

已验证：

- 当前构建产物中存在 `target/classes/agent/skills/data-processing-agent-skill/SKILL.md`。
- 已对 `DataProcessingReactAgentConfig.java` 执行定向 `javac` 编译检查并通过。

完整 Maven 编译未执行；当前工作树仍存在与本次调整无关的其它未提交改动。
