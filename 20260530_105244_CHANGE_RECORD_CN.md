# 2026-05-30 10:52:44 变更记录

## 变更背景

当前数据加工 ReAct Agent 暴露给模型的工具过多，包含大量 `load`、`validate`、`save` 细粒度工具，导致模型运行轮次长、流程不稳定，也容易绕开 skill 中期望的主流程。

本次变更将到“用户确认”为止的流程收敛为少量阶段型合并工具，让 Agent 只负责模板识别和字段绑定计划生成，确定性的校验、保存、确认项生成和阶段推进全部由工具完成。

## 主要变更

1. 收敛模型可见工具

   `DataProcessingAgentToolMethods` 中现在只暴露以下模型工具：

   - `prepare_task_context`
   - `load_template_catalog`
   - `accept_template_recognition`
   - `accept_field_binding_plan`
   - `submit_user_confirmation`
   - `mark_task_failed`

2. 合并确定性工具动作

   - `prepare_task_context` 合并任务状态加载、初始化、Excel 摘要读取和保存。
   - `accept_template_recognition` 合并模板识别校验、识别结果保存、模板包加载、必填字段加载和值集元数据加载。
   - `accept_field_binding_plan` 合并字段绑定计划校验、计划保存、确认项生成、确认项保存和阶段推进。
   - `submit_user_confirmation` 合并用户确认结果校验、保存和阶段推进。

3. 保留旧工具实现但不再暴露给模型

   旧的细粒度方法仍保留为普通 Java 方法，供内部确定性代码复用，但已经移除 `@Tool` 注解，并在方法注释中标明“原暴露给模型，现已废弃为模型工具”。

4. 重写 Agent Skill 流程

   `data-processing-agent-skill` 已改为严格少工具流程：

   - 第 1 步调用 `prepare_task_context`
   - 第 2 步调用 `load_template_catalog`
   - 第 3 步调用 `accept_template_recognition`
   - 第 4 步调用 `accept_field_binding_plan` 并停止
   - 用户确认提交分支调用 `submit_user_confirmation`

5. 保留关键业务规则

   Skill 中继续明确：

   - `USER_CONFIRM_OPTION` 和 `USER_CONFIRM_INPUT` 不进入字段绑定计划。
   - 字段绑定计划只覆盖 `DIRECT_MAPPING` 和 `EXPR` 的 `sourceColumns`。
   - 必填字段存在映射列但该列有空值时，必须生成手工输入确认项。
   - 用户输入后该目标字段整列全量覆盖，不是只覆盖空值行。

## 验证结果

已执行：

```bash
mvn -q -DskipTests compile
```

编译通过。

