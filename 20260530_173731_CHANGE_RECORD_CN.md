# 2026-05-30 17:37:31 变动说明

## 背景

本次变更围绕数据加工 React Agent 的流式响应接口进行调整。目标是让 `DataProcessingReactAgentService.run(...)` 直接返回 `Flux<AssistantMessage>`，上层接口或调用方再根据 `AssistantMessage` 转换为 SSE。

这样可以让 service 层保持 Spring AI 消息模型，不再对外暴露自定义流式事件对象，后续前端或接口层可以统一从 `AssistantMessage.getText()` 与 `AssistantMessage.getMetadata()` 中读取内容和事件类型。

## 修改内容

### `src/main/java/com/example/dataprocess/agent/service/DataProcessingReactAgentService.java`

#### `run(DataProcessingTaskRequest request)`

- 返回类型从同步结果：
  - `DataProcessingAgentResponse`
- 调整为流式消息：
  - `Flux<AssistantMessage>`
- Agent 调用方式从：
  - `dataProcessingReactAgent.call(...)`
- 调整为：
  - `dataProcessingReactAgent.stream(...)`
- 使用 `Flux.defer(...)` 延迟执行，确保每次订阅时才创建本次任务运行上下文。
- 使用 `RunnableConfig.builder().threadId(request.taskId()).build()` 保持基于任务编号的线程上下文。
- 新增运行时状态跟踪：
  - `AtomicReference<AssistantMessage> latestAssistantMessage`
  - `AtomicReference<OverAllState> latestState`
- 流式输出顺序调整为：
  - `START`
  - Agent 模型输出或工具调用事件
  - `FINAL`
  - 如异常则输出 `ERROR`

#### `toAssistantMessages(...)`

- 新增方法，用于把 Spring AI Alibaba Graph 的 `NodeOutput` 转换为一个或多个 `AssistantMessage`。
- 对 `StreamingOutput<?>` 做类型判断。
- 当流式消息是 `AssistantMessage` 时：
  - 记录最新 AssistantMessage。
  - 继续交给 `assistantMessages(...)` 生成对外消息。
- 当流式消息是 `ToolResponseMessage` 时：
  - 生成工具执行完成消息。
- 非流式输出或无法识别的消息类型不对外输出。

#### `assistantMessages(...)`

- 新增方法，用于处理模型侧 AssistantMessage。
- 如果模型消息包含工具调用：
  - 输出事件类型 `TOOL_CALL`。
  - metadata 中携带 `toolCalls` 和 `outputType`。
  - 保留原 AssistantMessage 的 tool calls 和 media。
- 如果模型消息包含文本：
  - 当 `OutputType` 为 `AGENT_MODEL_STREAMING` 时，输出事件类型 `MODEL_DELTA`。
  - 其他模型消息输出事件类型 `MODEL_MESSAGE`。
  - 文本内容写入 `AssistantMessage.getText()`。

#### `toolResponseMessage(...)`

- 新增方法，用于把 `ToolResponseMessage` 转换为 `AssistantMessage`。
- 输出事件类型 `TOOL_RESULT`。
- 文本内容格式为：
  - `工具调用完成: 工具名列表`
- metadata 中携带：
  - `outputType`
  - `toolNames`

#### `toFinalMessage(...)`

- 新增方法，用于在流式运行结束后生成最终响应消息。
- 输出事件类型 `FINAL`。
- `AssistantMessage.getText()` 写入最终 `DataProcessingAgentResponse` 的 JSON 字符串。
- metadata 中携带：
  - `response`

#### `resolveFinalResponse(...)`

- 新增方法，用于解析或恢复最终 `DataProcessingAgentResponse`。
- 优先使用最新 AssistantMessage 的文本解析 `DataProcessingAgentResponse`。
- 如果最新消息无法解析为合法最终响应：
  - 回退读取 `AgentStateTool` 中保存的任务状态。
  - 将持久化任务状态转换为 `DataProcessingAgentResponse`。
- 如果没有可恢复状态：
  - 返回 `REACT_AGENT_NO_FINAL_RESPONSE` 失败响应。

#### `toErrorMessage(...)`

- 新增方法，用于把异常转换为 `AssistantMessage`。
- 输出事件类型 `ERROR`。
- `AssistantMessage.getText()` 写入失败 `DataProcessingAgentResponse` 的 JSON 字符串。
- metadata 中携带：
  - `errorCode`
  - `response`

#### `toFailedResponse(...)`

- 从原 `run(...)` 内联异常处理逻辑中抽出。
- 对 `No AssistantMessage found` / `No AssitantMessage found` 异常单独处理：
  - 优先从任务状态恢复响应。
  - 无状态时返回 `REACT_AGENT_NO_ASSISTANT_MESSAGE`。
- 其他异常：
  - 标记任务状态为 `FAILED`。
  - 返回 `REACT_AGENT_RUN_FAILED`。

#### `latestAssistant(...)`

- 新增方法，用于从 `OverAllState` 的 `messages` 状态中倒序查找最新的 `AssistantMessage`。
- 用于解决流式结束时最后一条模型消息未直接保存在局部变量中的情况。

#### `assistantMessage(...)`

- 新增重载方法，用于统一构造对外输出的 `AssistantMessage`。
- 固定写入 metadata：
  - `event`
  - `taskId`
  - `node`，当节点名非空时写入。
- 支持合并原始 AssistantMessage metadata。
- 支持保留原始 AssistantMessage 的：
  - `toolCalls`
  - `media`

#### `textOrDefault(...)`

- 新增方法，用于工具调用消息没有文本内容时提供默认文案。

#### `responseMessage(DataProcessingAgentState state)`

- 补齐所有 `AgentWorkflowStage` 的中文响应文案。
- 去掉原先的 `default` 分支，使枚举阶段变更时由编译器辅助发现遗漏。

### `src/main/java/com/example/dataprocess/agent/interfaces/DataProcessingAgentInterface.java`

#### `run(DataProcessingTaskRequest request)`

- 返回类型从：
  - `DataProcessingAgentResponse`
- 调整为：
  - `Flux<AssistantMessage>`
- 接口声明从普通 POST：
  - `@PostMapping("/run")`
- 调整为 SSE 输出：
  - `@PostMapping(value = "/run", produces = MediaType.TEXT_EVENT_STREAM_VALUE)`
- 新增 imports：
  - `org.springframework.ai.chat.messages.AssistantMessage`
  - `reactor.core.publisher.Flux`
- 删除不再使用的 import：
  - `DataProcessingAgentResponse`

## 对外协议

`Flux<AssistantMessage>` 中每条消息的事件类型通过 metadata 表达：

- `START`：Agent 开始运行。
- `MODEL_DELTA`：模型流式增量文本。
- `MODEL_MESSAGE`：模型完整消息。
- `TOOL_CALL`：模型请求调用工具。
- `TOOL_RESULT`：工具调用完成。
- `FINAL`：最终业务响应。
- `ERROR`：异常响应。

调用方可按如下方式转换 SSE：

```java
agentService.run(request)
        .map(message -> ServerSentEvent.builder(message)
                .event(String.valueOf(message.getMetadata().get("event")))
                .build());
```

## 验证情况

- 已执行窄范围编译检查，通过：
  - `DataProcessingReactAgentService.java`
  - `DataProcessingAgentInterface.java`
- 已确认不存在 `DataProcessingAgentStreamEvent` 残留引用。

## 注意事项

- 本次未执行完整 Maven 编译，因为当前项目中 `DataProcessingStateGraphDefinition.java` 存在既有语法错误，会阻断全量编译。
- 工作区仍有其他未提交改动，本次提交仅包含流式响应相关 service、interface 和本变更说明文档。
