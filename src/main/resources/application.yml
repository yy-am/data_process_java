server:
  port: 8080

spring:
  application:
    name: data-process-java
  mvc:
    async:
      request-timeout: -1
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}
      chat:
        options:
          model: qwen-max

management:
  endpoints:
    web:
      exposure:
        include: health,info
