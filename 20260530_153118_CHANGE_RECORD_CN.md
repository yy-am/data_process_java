# 2026-05-30 15:31:18 Agent 阶段状态完善变更记录

## 变更背景

原 `AgentWorkflowStage` 只能覆盖确认前后的少量状态，无法清晰表达模板上下文准备、字段绑定计划校验、SQL 生成上下文准备、SQL 拼接完成和结果表写入等关键业务边界，影响 Agent 恢复执行、前端状态判断和 skill 分支指引。

## 变更内容

- 完善 `AgentWorkflowStage`，新增可恢复业务阶段：
  - `TASK_CONTEXT_READY`
  - `TEMPLATE_CONTEXT_READY`
  - `FIELD_BINDING_PLAN_READY`
  - `SQL_GENERATION_CONTEXT_READY`
  - `PROCESSING_SQL_RENDERED`
  - `RESULT_TABLE_WRITTEN`
- 为阶段枚举补充中文语义注释，明确阶段只表达外部可观察、可恢复的业务边界。
- 调整 Agent 工具阶段推进：
  - 解析文件摘要保存后进入 `TASK_CONTEXT_READY`。
  - 模板识别和模板上下文加载完成后进入 `TEMPLATE_CONTEXT_READY`。
  - 字段绑定计划校验保存后进入 `FIELD_BINDING_PLAN_READY`。
  - 完整 `INSERT SELECT` SQL 拼接完成后进入 `PROCESSING_SQL_RENDERED`。
- 补齐 `nextAction` 和标准响应文案对新增阶段的覆盖。
- 同步更新 `data-processing-agent-skill` 中的阶段定义、阶段分支和 SQL 拼接完成后的返回阶段说明。

## 未包含内容

- 本次提交不包含流式响应相关改动。
- 本次提交不包含其他非 Agent 状态阶段相关的工作区变更。

## 验证情况

- 已对 Agent 状态相关 Java 文件做窄编译验证。
- 全量 Maven 编译仍受既有 `DataProcessingStateGraphDefinition.java` 语法错误影响，未作为本次变更处理范围。
