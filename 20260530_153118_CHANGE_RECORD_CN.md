# 2026-05-30 15:31:18 Agent 阶段状态完善变更记录

## 变更背景

原 `AgentWorkflowStage` 只能覆盖确认前后的少量状态，无法清晰表达模板上下文准备、字段绑定计划校验、SQL 生成上下文准备、SQL 拼接完成和结果表写入等关键业务边界，影响 Agent 恢复执行、前端状态判断和 skill 分支指引。

## 变更内容

### 新增

#### `src/main/java/com/example/dataprocess/agent/model/AgentWorkflowStage.java`

- 在枚举 `AgentWorkflowStage` 中新增阶段：
  - `TASK_CONTEXT_READY`：解析文件摘要已加载，可以进入模板识别。
  - `TEMPLATE_CONTEXT_READY`：模板识别、标准模板、加工规则、必填字段和值集元数据均已准备完成。
  - `FIELD_BINDING_PLAN_READY`：字段绑定计划已接收并校验。
  - `SQL_GENERATION_CONTEXT_READY`：原始 Excel 数据已写入临时表，SQL 生成上下文已准备好。
  - `PROCESSING_SQL_RENDERED`：已完成 `ProcessingPlanDsl` 校验和完整 `INSERT SELECT` SQL 拼接，尚未执行落表。
  - `RESULT_TABLE_WRITTEN`：结果表写入已执行。

### 修改

#### `src/main/java/com/example/dataprocess/agent/model/AgentWorkflowStage.java`

- 修改类注释，明确 `AgentWorkflowStage` 表达的是“外部可观察、可恢复的业务边界”，不是工具内部每个细粒度动作。
- 为所有枚举值补充中文语义注释。
- 保留既有阶段：
  - `RECEIVED`
  - `TEMPLATE_RECOGNIZED`
  - `CONFIRMATION_ANALYZED`
  - `USER_CONFIRMATION_REQUIRED`
  - `USER_CONFIRMED`
  - `FAILED`
  - `COMPLETED`

#### `src/main/java/com/example/dataprocess/agent/tool/DataProcessingAgentToolMethods.java`

- 修改方法 `prepareTaskContext(String taskId, String parsedFileRef)`：
  - 当解析文件摘要首次加载并保存后，将任务阶段推进到 `TASK_CONTEXT_READY`。
  - 当历史状态已存在解析文件摘要但阶段仍是 `RECEIVED` 时，将任务阶段补正为 `TASK_CONTEXT_READY`。

- 修改方法 `acceptTemplateRecognition(String taskId, TemplateRecognitionResult result)`：
  - 模板识别结果校验、模板包加载、必填字段加载和值集元数据加载完成后，阶段从原来的 `TEMPLATE_RECOGNIZED` 调整为 `TEMPLATE_CONTEXT_READY`。

- 修改方法 `acceptFieldBindingPlan(String taskId, FieldBindingPlan plan)`：
  - 字段绑定计划校验通过后，先将阶段推进到 `FIELD_BINDING_PLAN_READY`。
  - 后续仍根据确认项是否为空进入 `USER_CONFIRMATION_REQUIRED` 或 `USER_CONFIRMED`。

- 修改方法 `executeProcessingPlan(String taskId, AgentSqlGenerationContext sqlGenerationContext, ProcessingPlanDsl processingPlanDsl)`：
  - 在 `ProcessingPlanSqlTool.renderInsertSelectSql(...)` 成功返回后，将任务阶段推进到 `PROCESSING_SQL_RENDERED`。
  - 增加追踪日志：`完成 SQL 片段校验并拼接完整 INSERT SELECT SQL。`
  - 该方法仍只负责 SQL 校验和拼接，不执行数据库落表。

- 修改方法 `saveParsedExcelSummary(String taskId, ParsedExcelSummary summary)`：
  - 保存解析文件摘要后同步推进到 `TASK_CONTEXT_READY`。
  - 该方法仍是旧细粒度工具的 Java 内部保留能力，不重新暴露给模型。

- 修改方法 `saveTemplateContext(String taskId, TemplateBundle templateBundle, StandardRequiredFields requiredFields, List<ValueSetMetadata> valueSetMetadata)`：
  - 保存模板上下文后同步推进到 `TEMPLATE_CONTEXT_READY`。
  - 该方法仍是旧细粒度工具的 Java 内部保留能力，不重新暴露给模型。

- 修改方法 `saveFieldBindingPlan(String taskId, FieldBindingPlan plan)`：
  - 保存字段绑定计划后同步推进到 `FIELD_BINDING_PLAN_READY`。
  - 该方法仍是旧细粒度工具的 Java 内部保留能力，不重新暴露给模型。

- 修改方法 `nextAction(DataProcessingAgentState state)`：
  - 补充 `TASK_CONTEXT_READY`、`TEMPLATE_CONTEXT_READY`、`FIELD_BINDING_PLAN_READY`、`CONFIRMATION_ANALYZED`、`SQL_GENERATION_CONTEXT_READY`、`PROCESSING_SQL_RENDERED`、`RESULT_TABLE_WRITTEN` 的下一步提示。
  - 移除兜底 `default` 分支，改为显式覆盖所有枚举阶段。

- 修改方法 `responseMessage(DataProcessingAgentState state)`：
  - 补充新增阶段的标准中文响应文案。
  - 移除兜底 `default` 分支，改为显式覆盖所有枚举阶段。

#### `src/main/resources/agent/skills/data-processing-agent-skill/SKILL.md`

- 修改“运行总则”：
  - 新增 `PROCESSING_SQL_RENDERED` 作为可直接停止并返回的阶段。
  - 新增完整阶段含义说明，覆盖 `TASK_CONTEXT_READY`、`TEMPLATE_CONTEXT_READY`、`FIELD_BINDING_PLAN_READY`、`SQL_GENERATION_CONTEXT_READY`、`PROCESSING_SQL_RENDERED`、`RESULT_TABLE_WRITTEN`。

- 修改“第 1 步：准备任务上下文”的阶段分支：
  - `USER_CONFIRMED` 进入第 6 步。
  - `SQL_GENERATION_CONTEXT_READY` 进入第 7 步。
  - `PROCESSING_SQL_RENDERED`、`RESULT_TABLE_WRITTEN`、`FAILED`、`COMPLETED` 直接返回 `agentResponse`。
  - `RECEIVED`、`TASK_CONTEXT_READY` 进入第 2 步。
  - `TEMPLATE_RECOGNIZED`、`TEMPLATE_CONTEXT_READY`、`FIELD_BINDING_PLAN_READY`、`CONFIRMATION_ANALYZED` 进入第 3 步或第 4 步中尚未完成的步骤。

- 修改“第 4 步：提交字段绑定计划”的返回分支：
  - 补充 `PROCESSING_SQL_RENDERED` 的停止返回规则。

- 修改“第 5 步：处理用户确认提交”的返回分支：
  - 补充 `PROCESSING_SQL_RENDERED` 的停止返回规则。

- 修改“第 8 步：提交加工计划并写入结果表”的返回规则：
  - 明确当前只完成 SQL 拼接时，最终响应阶段必须使用 `PROCESSING_SQL_RENDERED`。
  - 明确 `summary.insertSelectSql` 必须包含完整 SQL。

- 修改最终返回协议示例：
  - 将“当前仅完成 SQL 拼接、尚未接入落表执行时”的示例阶段从 `USER_CONFIRMED` 改为 `PROCESSING_SQL_RENDERED`。

### 删除

- 本次未删除任何类、方法或文件。

## 未包含内容

- 本次提交不包含流式响应相关改动。
- 本次提交不包含其他非 Agent 状态阶段相关的工作区变更。

## 验证情况

- 已对 Agent 状态相关 Java 文件做窄编译验证。
- 全量 Maven 编译仍受既有 `DataProcessingStateGraphDefinition.java` 语法错误影响，未作为本次变更处理范围。
