# 2026-05-28 17:12:44 修改记录

## 1. 本次改动范围

本次提交主要处理三类调整：

1. 模板识别阶段改为把完整模板目录 Markdown 原文交给 AI。
2. 清理当前代码中确认无调用方的旧方法和预留接口。
3. 撤回未提交的弹性域写入类，不再把弹性域落库逻辑纳入当前代码。

## 2. 新增文件

1. `20260528_171244_CHANGE_RECORD_CN.md`
   - 作用：记录本次提交的新增、修改、删除内容。

## 3. 修改文件

### 3.1 模板识别链路

1. `src/main/java/com/example/dataprocess/infrastructure/service/TemplateCatalogService.java`
   - 新增 `readTemplateCatalogMarkdown()`。
   - 作用：读取完整 `TEMPLATE_CATALOG.md` 原文，供模板识别阶段直接传给 AI。
   - 原结构化解析能力保留，用于后置校验模板编码和映射关系。

2. `src/main/java/com/example/dataprocess/infrastructure/service/TemplateRecognitionService.java`
   - AI 输入从 `presetTemplates`、`standardTemplates` 结构化列表调整为完整 `templateCatalogMarkdown`。
   - `readPresetTemplateCatalog()` 仍保留在识别后校验环节，防止 AI 返回目录外模板或错误映射。
   - 补充中文类注释、方法注释和关键逻辑说明。

3. `src/main/resources/prompts/template-recognition-prompt.md`
   - 将提示词中的模板来源改为完整模板目录 Markdown。
   - 明确 `templateCatalogMarkdown` 中包含预置模板、标准模板和映射关系。

### 3.2 未使用代码清理

1. `src/main/java/com/example/dataprocess/infrastructure/service/ExcelParsingService.java`
   - 删除未使用的 `parse(File, ...)` 重载。
   - 删除未使用的 `parse(byte[], ...)` 重载。

2. `src/main/java/com/example/dataprocess/infrastructure/service/PromptTemplateService.java`
   - 删除未使用的 `renderPrompt(...)` 方法。

3. `src/main/java/com/example/dataprocess/infrastructure/service/StructuredConfirmationService.java`
   - 删除旧兼容方法 `buildUserConfirmationItems(...)`。
   - 当前调用方统一使用 `buildUserConfirmation(...)`，以便同时获得 `ProcessingRule`、`VagueBindingRecoResult` 和 `UserConfirmationItems`。

### 3.3 设计与进度文档

1. `CURRENT_WORKFLOW_DESIGN_CN.md`
   - 撤回 `ElasticDomainTableWriter` 相关设计描述。
   - 恢复为“第一段 Excel 到弹性域临时表暂不由当前工作流实现，本工作流只消费后续传入的 actual/elastic 映射”。

2. `0527_TODO_CN.md`
   - 撤回 `ElasticDomainTableWriter` 作为 actual/elastic 映射来源的描述。
   - 保留后续需要明确 actual/elastic 映射写入 state 的待办。

3. `CODEX.md`
   - 删除 `ElasticDomainTableWriter` 进度记录。
   - 补充模板识别上下文调整和未使用代码清理记录。

## 4. 删除文件

1. `src/main/java/com/example/dataprocess/infrastructure/service/ResultExportService.java`
   - 删除原因：当前没有实现类、注入点或调用方，只是旧的 exportA 预留接口。
   - 当前工作流暂不实现导出链路。

## 5. 弹性域代码处理

1. `src/main/java/com/example/dataprocess/infrastructure/service/ElasticDomainTableWriter.java`
   - 该文件是本次提交前的未跟踪文件。
   - 根据最新决定，弹性域相关代码暂不需要，已从工作区删除，不纳入本次提交。

## 6. 本次不纳入提交的工作区残留

以下文件仍保持未提交状态，不属于本次改动范围：

1. `.gitignore`
2. `.mvn/maven.config`
3. `.mvn/settings.xml`

## 7. 验证结果

已执行：

```powershell
mvn -q -DskipTests compile
```

结果：编译通过。

